selectors:
  - name: dim_except_listings_w_hosts
    description: All dimension models except dim_listings_w_hosts
    definition:
      union:
        - method: path
          value: models/dim
        - exclude:
          - method: fqn
            value: dim_listings_w_hosts